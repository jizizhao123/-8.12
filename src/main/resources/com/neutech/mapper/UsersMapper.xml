<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neutech.mapper.UsersMapper">
    <select id="getPersonalRanking" parameterType="java.lang.Integer" resultType="java.lang.Integer">
        SELECT ranking FROM (
                                SELECT id, ROW_NUMBER() OVER (ORDER BY score DESC) AS ranking
                                FROM users
                            ) AS ranked_users
        WHERE id = #{userId}
    </select>
</mapper>
